sudo: required

language: python

dist: trusty

before_install:
  - sudo pip install codecov
  
after_success:
  - codecov
  
 addons:
  sonarqube:
    organization: "laurelmcintyre-github" 
    token:
      secure: •••••••••••••••• 

script:
  - coverage run mytan.py
  - codecov
  - sonar-scanner
